[project]
name = "bank-marketing"
version = "0.1.0"
description = ""
authors = [
    {name = "Adonai",email = "joseadonai.jr@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[tool.poetry]

[tool.poetry.group.dev.dependencies]
scikit-learn = "1.6.1"
plotly = "6.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
